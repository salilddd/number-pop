# Minification is off for release (see build.gradle.kts), so this file is a
# placeholder. If it is ever turned on, note that anything the page reaches
# through @JavascriptInterface has to be kept by name -- R8 has no way to see
# a call coming from inside the WebView.
