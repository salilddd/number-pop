<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      There is deliberately no <uses-permission> here, INTERNET included.
      Every byte the game needs ships inside the APK and is served to the
      WebView by WebViewAssetLoader, so the app can run with no network
      access at all. Anything that tries to reach out is refused in
      MainActivity rather than merely failing.
    -->

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NumberPop">

        <!--
          Portrait, because the whole board is laid out tall: the question
          sits above a play field the bubbles rise through, and on a phone
          held sideways there is no room left between the two. A child who
          tips the phone mid-question should not lose the question.

          configChanges matters more than it looks, and still does with the
          lock on. Without it a configuration change tears the activity down
          and rebuilds it, which reloads the WebView and ends the run
          mid-question — and the lock does not stop all of them: multi-window
          and font-size changes still arrive, and from Android 16 the system
          ignores an orientation lock on large screens, so a tablet or an
          unfolded foldable can still hand us a landscape window. The game
          re-lays itself out from window.resize either way, so it wants the
          change handed to it rather than restarted around it.
        -->
        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|keyboardHidden|density|uiMode"
            android:exported="true"
            android:theme="@style/Theme.NumberPop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
